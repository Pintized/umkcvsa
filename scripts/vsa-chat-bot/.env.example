# Copy to .env (same folder) and fill in. NEVER commit the real .env.
DISCORD_BOT_TOKEN=your-bot-token-from-discord-dev-portal
ANTHROPIC_API_KEY=your-anthropic-api-key
SUPABASE_URL=https://wrlpsetbkeyoyamkopgf.supabase.co
SUPABASE_SERVICE_ROLE_KEY=service-role-key-from-supabase-dashboard
# one channel id, or several separated by commas: 111,222,333
VSA_CHAT_CHANNEL_ID=the-dedicated-channel-id
# optional: per-channel model overrides (aliases: haiku, sonnet, opus)
# VSA_CHANNEL_MODELS=111=opus,222=sonnet
# optional: model for unlisted channels (default: haiku)
# VSA_DEFAULT_MODEL=haiku
